# 敌人「实渲 0 像素」诊断记录（MoShan / MoGuai / MoGu）—— ★ 已定位、✅ 已修复

> **✅ 修复状态（09-17）**：三个 Ziyuan 敌人的骨骼引用**已全部接回**，实渲像素
> MoShan **0.000% → 6.269%**、MoGuai **0.000% → 9.301%**、MoGu 只留 SMR **0.000% → 4.884%**。
> **修法不是重导 FBX** —— 上游 FBX 全部健康，坏的是 prefab 里被写死成 `{fileID: 0}` 的引用。
> 完整修复过程、对账表与验证见**第四节**。
>
> **根因（已确认）**：这两个 prefab 的 `SkinnedMeshRenderer` 里，**骨骼引用数组 `m_Bones` 全部丢失**
> （元素全为 null、`rootBone` 也为 null）⇒ 蒙皮结果退化、三角形不被光栅化 ⇒ **一个像素都不画**。
>
> **网格、材质、shader、bindposes、取景、可见性、剔除设置全部无罪**（逐项实测排除，见第二节）。
> 同一份网格 + 同一个材质，**只把渲染器换成普通 `MeshRenderer` 就完整显示**。
>
> **★ 已在演示场（`Showcase.unity`）用正常相机复核，结论不变** —— 见「一·补」。
> 顺带把原先唯一的"反例"(`Z_Enemy_MoGu`) 解释清楚了：它看得见靠的是身上 4 块普通 `MeshRenderer`，
> 它的 SMR 同样是 0.000%。**判据成立、没有反例。**
>
> 起点：`Z_Enemy_MoShan`（14/14 网格齐全）与 `Z_Enemy_MoGuai`（10/10）实渲 0 像素，
> 而 `Tools/cs/chk_enemy_vis.cs` 判它们 ✅（它只查 `sharedMesh != null`）。

---

## 一、根因证据链

### 1. 普查：骨骼引用与「能否渲染」完全对应

`Tools/cs/dg_ab_shot.cs` 遍历 `Assets/_Project/Prefabs/` 下所有敌人/龙 prefab：

| prefab | SMR | 有骨骼的 SMR | 骨骼元素总数 | 非 null | 实渲 |
|---|---|---|---|---|---|
| `Enemy_MoOu` | 8 | 8/8 | 184 | **184** | ✅ |
| `Enemy_MoTu` | 9 | 9/9 | 207 | **207** | ✅ |
| `Enemy_MoYan` | 8 | 8/8 | 184 | **184** | ✅ |
| `Z_Dragon` | 2 | 2/2 | 546 | **546** | ✅ |
| `Z_Enemy_MoLong` | 2 | 2/2 | 546 | **546** | ✅ |
| **`Z_Enemy_MoShan`** | 14 | **0/14** | 364 | **0** | ❌ |
| **`Z_Enemy_MoGuai`** | 10 | **0/10** | 970 | **0** | ❌ |
| `Z_Enemy_MoGu` | 3 | 0/3 | 240 | 0 | ✅ ⚠️ 唯一例外 |

**规律：非 null 骨骼数 > 0 ⇒ 渲染正常；= 0 ⇒ 不渲染。**
`Z_Enemy_MoGu` 是唯一反例（全 null 却仍渲染），第五节单列。

### 2. 三档对照实验（`Tools/cs/dg_smr_rebuild.cs`）

在 MoShan 实例旁新建三个测试对象，轮流单独拍摄：

| 测试 | 构造 | 非背景 |
|---|---|---|
| **T1** | 全新 SMR + MoShan 的 mesh + 材质，**不给骨骼** | **0.00%** |
| **T2** | 全新 SMR + MoGu 的 mesh + 材质，**不给骨骼**（对照） | **0.00%** |
| **T3** | 全新 SMR + **同一份** MoShan mesh + **26 个有效骨骼引用**（都指向自身） | **1.37%** ✅ |

- **T2=0** ⇒「新建 SMR 不给骨骼就画不出来」是 Unity 的正常行为，实验有效；
- **T3 出现模型** ⇒ **只补上有效骨骼引用，同一份网格立刻能画** ⇒ 根因就是骨骼引用。

### 3. 同取景 A/B（`Tools/cs/dg_ab_shot.cs`，给用户看的证据图）

同一个实例、同一台相机、**同一视野 9.43 m**，唯一变量是渲染路径：

| | 渲染路径 | 非背景 | 量化色数 |
|---|---|---|---|
| A | 原样 `SkinnedMeshRenderer` | **0.00%** | 2（全是蓝底抖动值） |
| B | 同一网格 + 同一材质挂 `MeshRenderer` | **1.56%** | 37（完整山怪 + 黑墨轮廓） |

产出 **`Tools/screenshots/enemies/_对比_同网格_SMR空白_vs_MeshRenderer画出.png`**。

---

## 一·补、演示场复核（正常相机 · 同 tick 差分帧）—— ★ 口径最终版

上面第一轮是在**正式关卡里自搭相机/灯光/地面**量的。用户当场指出"**为什么非得要弄个背景，
正常的摄像机渲染不就好了**"，并给出 Game 视图截图。这个批评是对的，于是全部重做：

- **场景**：`Assets/_Project/Scenes/Showcase.unity`（演示场）。台子/相机/三盏灯/地面材质
  全部来自 `ActionShowcase.BuildStage()` —— **就是游戏里那一套**，不再自搭。
- **判据**：**同 tick 背靠背 `Camera.Render()`**，被测渲染器【开】/【关】各渲一帧、逐像素求差。
  **不用任何哨兵底色**（画面就是正常渲染结果），两帧都关掉被测对象的投影 ⇒ 差集是**纯几何像素**。
  额外自检：`GeometryUtility.TestPlanesAABB` 报「是否在视锥内」+ 把相机位置打进报告。
- **阳性对照**：同一批里放墨龙（骨骼 273/273 有效）。

`Tools/cs/dg_sc_verify.cs` 实测（1280×720 = 921600 px）：

| prefab | 构成 | 全部（SMR+MR） | 只留 SMR | 只留 MeshRenderer |
|---|---|---|---|---|
| `Z_Enemy_MoShan` | 14 SMR（骨骼 **0/14**）／ 0 MR | **0.000%**（两帧逐字节相同） | — | — |
| `Z_Enemy_MoGuai` | 10 SMR（**0/10**）／ 1 MR | **0.000%**（仅 1 px 噪点） | **0.000%** | 0.000%（就是那 1 px） |
| `Z_Enemy_MoGu` | 3 SMR（**0/3**）／ **4 MR** | **1.930%** | **0.000%** | **1.930%** |
| `Z_Enemy_MoLong` | 2 SMR（**273/273**）／ 0 MR | **2.063%** ✅ | — | — |

**三点结论**：
1. 在**真实测试场景 + 正常相机**下，`MoShan` / `MoGuai` 依然**一个像素都不画**（多档是逐字节相同）
   ⇒ 第一轮的结论**不是自搭板子造成的假象**，根因站得住。
2. **`MoGu` 的"反面例子"解释清楚了**：它看得见完全靠身上那 **4 个普通 `MeshRenderer`**
   （`Object_28/34/43/90`，静态网格，不需要骨骼）。**把它那 3 个 SMR 单独拿出来 = 0.000%。**
   ⇒ 「非 null 骨骼 = 0 ⇒ 不画」**没有反例**，判据成立。
3. 墨龙 2.063% 是**同一套相机、同一套代码**下读出来的，证明这次度量本身是有效的。

**产物**：
- `Tools/screenshots/scene/_对比_演示场正常相机_四个对象_开vs关.png`（五组开/关，结论一目了然）
- `Tools/screenshots/scene/SV_<prefab>_<档>_开/关.png`
- `Tools/cs/dg_sc_vis.cs`（在演示场**条目内**量，走 `PlayForCapture`）
- `Tools/cs/dg_sc_verify.cs`（自摆 + 对准 + 视锥自检 + 拆档）
- `Tools/make_sc_compare.py`

**顺带发现一个演示场的真 bug**：`ActionShowcase.CollectItems()` 里
`AddEnemy("敌人 墨偶（远程）", "Z_Enemy_MoOu", …)` 与 `AddEnemy("敌人 墨魇（精英）", "Z_Enemy_MoYan", …)`
写的 prefab 名**不存在**（实际文件是 `Enemy_MoOu.prefab` / `Enemy_MoYan.prefab`，没有 `Z_` 前缀）
⇒ `LoadEnemy` 返回 null ⇒ `Play()` 走 `actor = _player` 分支
⇒ **面板显示「已就位」，其实是在驱动主角，根本没召唤敌人**。⬜ 待修

---


## 二、排除清单（都已实测，别再重复）

| 假设 | 证伪方式 | 结果 |
|---|---|---|
| 取景太紧 / 太远 | 包围盒 ×1.5（9.5 m）、固定 6 m、正交 5→5000 m 扫描 | 全 0，非取景 |
| 几何跑到别的尺度 | 同上（跨 1000 倍读数不变） | 排除 |
| `localBounds` 撒谎 | 改用 `mesh.bounds` 世界化取景（B 档） | 仍 0，排除 |
| 视锥剔除 | `updateWhenOffscreen=true` + `localBounds` 设超大，三档对照 | 三档全 0，且**三档图字节数完全相同** |
| 材质丢了 | `chk_mat_state.cs`：材质槽 null 数 = 0（14/10/3/2） | 材质都在 |
| 材质属性异常 | 全量 dump shader 属性（`_BaseMap`/`_BaseColor`/renderQueue/…） | 三个材质几乎一致，无 null、无 alpha=0、非 Transparent |
| 材质本身 | **交叉互换**：MoShan 换成 MoGu 材质副本后重拍 | 仍 0.00%，材质无罪 |
| 贴图 | `_BaseMap` 均 2048² DXT1 有内容 | 正常 |
| 网格缺面 | `subMeshCount` / `GetIndexCount` | MoShan 合计 51921、MoGuai 64734，**索引齐全** |
| 网格数据本身 | **把同一 mesh 挂普通 `MeshRenderer`** | **完整山怪显示出来**，网格无罪 |
| `bindposes` 把顶点搬走 | 复制 mesh、`bindposes` 全设单位矩阵后重拍 | 仍 0.00%，排除 |
| blendShape 权重异常 | `m_BlendShapeWeights` 全量 dump + 清空实验 | 三家都是 size=0，排除 |
| SMR 其他序列化字段 | 完整展开（`NextVisible(true)`）后 diff | 除 `m_Mesh`/`m_Materials`/`m_AABB` 外**零差异** |
| `quality` / `forceMatrixRecalculation` | 直接读 | 三家同为 `Auto` / `False` |

> **★ 方法论教训**：第一轮 `SerializedObject` dump 用了 `NextVisible(enter: false)`，
> **数组子元素根本没被展开** —— `m_Bones` / `m_Materials` / `m_BlendShapeWeights` 全是盲区，
> 所以当时"字段零差异"的结论是**假的**。**dump 结构化对象必须 `Next(true)` 展开数组。**

---

## 三、两个度量坑（本轮的副产物，比结论更通用）

### 坑 1：**8-bit RenderTexture 有 dithering** —— 哨兵色要选「抖动 < 阈值」的

| 哨兵色 | 抖动后的实际取值 | 最大偏差 | 阈值 24 下 |
|---|---|---|---|
| 绿 `(0,255,0)` | `(0,240,0)` … `(44,240,44)` | **62** | ❌ 恒定 **25.63% 假「非背景」** |
| 蓝 `(0,0,255)` | `(0,0,234)` / `(0,0,240)` | 21 | ✅ 干净 |
| 红 `(255,0,0)` | `(240,0,0)` / `(236,0,0)` | 19 | ✅ 干净 |

**识别指纹：换取景尺度、换模型，读数一模一样 ⇒ 那是噪声。**

### 坑 2：**哨兵色绝不能用洋红 `(255,0,255)`**

**那正是 Unity「材质丢失」时渲染的颜色。** 用它当画布 ⇒ 看图的任何人（包括我自己）
都无法区分「我铺的底」与「真的缺材质」。实测代价：用户看到一片洋红直接问「为什么会材质丢失」，
我自己也据此误判过一轮。**哨兵色一律用纯蓝；需要第二底色时用纯红。**

> 连带纪律：**给临时物选色前，先问「这个颜色在 Unity 里是不是有既定含义」。**

### 坑 3：**「零像素 / 零差异」这类结论天然需要对照物**

单张全空图是零信息量 —— 用户看不到「这儿本该有东西」。
做法：**并排放一个「正常的」**（同底色、同口径），顶部叠状态带与读数，底部一句话说清哪块是模型。
参考 `Tools/make_compare_ab.py`（Pillow 拼图）。
附带小坑：**微软雅黑没有 ✅/❌ 字形**（渲成空心方块），状态标记要用 `ImageDraw.rectangle` 画色块。

### 附带硬事实

1. **`mesh.vertices` 在本工程返回空数组**（`isReadable=false`；`vertexCount`/`bindposes` 仍可读）
   ⇒「读顶点真值」这条手段不可用。`mesh.triangles.Length` 也因此为 0（但渲染不受影响）。
2. **`renderer.bounds` = `localBounds` × `lossyScale`**（推的，非蒙皮真值）；
   且 `localBounds` 与 `mesh.bounds` 不一致（被显式设置过）。
3. **「`bones` 长度对、元素全 null」这种状态，`sharedMesh != null` 类判据完全查不出。**

---

## 四、修复 —— ✅ 已完成（方案 B 的真身不是"重导 FBX"）

**用户拍板 B（重导 FBX 修骨架）。实做时发现真正要修的不是 FBX** ——
上游 FBX 一个字节都不用动，坏的是 prefab 里被写死成 `{fileID: 0}` 的引用。

### 4.1 对账（`Tools/cs/bf_tree.cs`，只读 dump prefab 层级 + 源 FBX）

每个敌人 prefab **就是对应 Ziyuan FBX 的完整拷贝**，骨架 / 网格 / 数量严丝合缝：

| 敌人 prefab | 源 FBX | prefab 骨架 | SMR | 源 FBX 骨骼 | 源 SMR 状态 |
|---|---|---|---|---|---|
| `Z_Enemy_MoShan` | `mountain_orge.fbx` | `_rootJoint`→`mixamorig:*` | 14（`Object_33..59`） | 26 | **26/26 ✅** |
| `Z_Enemy_MoGuai` | `low-poly_orc.fbx` | `_rootJoint`→`Bip001_01` | 10（`Object_7..25`） | **97** | **97/97 ✅** |
| `Z_Enemy_MoGu` | `cursed_undead_soldier_rig.fbx` | `GLTF_created_0_rootJoint`→`Bip001_01` | 3（`Object_21/23/24`） | 80 | **80/80 ✅** |

骨架树在 prefab 内是**逐节点同名的本地副本**（`m_CorrespondingSourceObject: {fileID: 0}`），
**唯一的破坏**是每个 SMR 的 `m_Bones` 被写成 **364 / 970 / 240 条 `{fileID: 0}`**，
`m_RootBone` 也是 `{fileID: 0}`（合计 **1574** 条）。

### 4.2 修法（`Tools/cs/bf_fix.cs`）

按 **源 FBX 里同名 SMR 的 `bones` 顺序**，把 **prefab 内同名 Transform** 接回去：

```
LoadPrefabContents(prefab)
  → 对每个 SMR：smr.bones = [按源 FBX 顺序查到的同名 Transform...]
                smr.rootBone = prefab 内的 _rootJoint / GLTF_created_0_rootJoint
  → SaveAsPrefabAsset
```

- **顺序必须照抄源 FBX** —— 它必须与 `mesh.bindposes` 的顺序一致，**不能自己排序**。
- 先跑 **只读预演**：三条全部 `26/26`、`97/97`、`80/80`，**缺骨骼引用 0、缺 rootBone 0**。
- 改动形态**极干净**：1574 条 `- {fileID: 0}` 被替换 + 27 条 `m_RootBone`（=14+10+3），
  **零净增删行（1663/1663）**，无一条改动带 guid，未触碰 `m_Mesh` / `m_Name` / 层级，
  `prefabType` 与 `childCount` 全程不变 ⇒ `LoadPrefabContents` 没有把嵌套 prefab 实体化。

### 4.3 验证

**（a）静态判据**（`Tools/cs/chk_enemy_vis.cs`，7/7 通过）：
MoShan **364/364**、MoGuai **970/970**、MoGu **240/240**，空材质槽 0。

**（b）演示场相机 + 同 tick 差分帧**（`Tools/cs/dg_sc_verify.cs`，1280×720 = 921600 px）：

| 对象 | 修复前 | 修复后 |
|---|---|---|
| `Z_Enemy_MoShan` | 0.000%（两帧逐字节相同） | **6.269%** |
| `Z_Enemy_MoGuai` | 0.000% | **9.301%** |
| `Z_Enemy_MoGu` 只留 SMR | **0.000%** | **4.884%** |
| `Z_Enemy_MoGu` 只留 MR | 1.930% | 2.795% |
| `Z_Enemy_MoLong`（阳性对照） | 2.063% | 2.373% |

**（c）演示场真实路径**（`Tools/cs/dg_sc_vis.cs`，走 `PlayForCapture` 逐条目召唤）：
六个条目全部召唤成功，纯几何像素 墨徒 1.015% / 墨偶 0.860% / 墨魇 1.329% /
墨山 1.407% / 墨骨 0.494% / 墨龙 2.964%；六个目标「骨骼全 null 的 SMR」**全部为 0**。

### 4.4 备选方案（未采用，留档）

| 方案 | 做法 | 结论 |
|---|---|---|
| A | 把 `SkinnedMeshRenderer` 换成 `MeshFilter` + `MeshRenderer` | 可行，但**没必要**：骨架在 prefab 里是完整的，接线更省事，且不影响任何按 SMR 查对象的代码 |
| **B（采用）** | **接回骨骼引用** | ✅ 已实施 |
| B′ | 回 Blender/重导 FBX | **实测不需要** —— FBX 全部健康 |
| C | 运行时兜底给 SMR 填骨骼 | 会把坑埋进运行时，不采用 |

**⚠️ 方案 A 的前置检查结论（保留，供将来真要用 A 时参考）：**
- ✅ `Rendering/InkMaterialForcer.cs:64,92`、`UI/InkMaterialSwap.cs:30` 找渲染器用的是**泛型 `Renderer`** ⇒ 换 `MeshRenderer` 不会踩空。
- ✅ `Enemies/EnemyDragon.cs` 里 6 处 SMR 查找**只服务墨龙**（骨骼 546/546 有效），不受影响。
- ⚠️ **真正的阻塞点在测试代码**：`Utils/PlaytestHarness.cs:2883 / 2906 / 2910` 三处写死了
  `GetComponentInChildren<SkinnedMeshRenderer>(true)` ⇒ 换 `MeshRenderer` 会让 S4 断言**假红**。
  **因为走了 B，这三处不用改。**

### 4.5 顺带修掉的两个东西

- **演示场面板笔误**：`ActionShowcase` 里墨偶/墨魇写成 `Z_Enemy_MoOu` / `Z_Enemy_MoYan`，
  实际文件是 `Enemy_MoOu.prefab` / `Enemy_MoYan.prefab`（**没有 `Z_` 前缀**）⇒ `LoadEnemy` 返回 null
  ⇒ `Play()` 静默退回 `actor = _player` ⇒ 面板显示"已就位"其实在驱动主角。**已修正**，
  并补上墨山/墨骨两组条目（`ItemCount` 23 → 29）。
- **验收判据**：`chk_enemy_vis.cs` 旧版只看 `sharedMesh != null` —— **正是它把这坑放过去的**。
  新版三条并列：① `sharedMesh != null` ② **SMR 的 `bones` 逐元素非 null 数 > 0** ③ 材质槽无空槽。

---

## 五、原先的"未解项"（`Z_Enemy_MoGu` 反例）—— 已解决

**已解释，见「一·补」第 3 行**：`MoGu` 能渲染**不是因为它的 SMR 画出来了**，而是因为它身上
除了 3 个 SMR **还有 4 个普通 `MeshRenderer`**（`Object_28` vc=70 / `Object_34` vc=103 /
`Object_43` vc=103 / `Object_90` vc=468，共 744 顶点）。
拆开量（修复前）：**只留 SMR = 0.000%（两帧逐字节相同）**，**只留 MeshRenderer = 1.930%**。
⇒ 骨骼引用接回后同一档：**只留 SMR = 4.884%**、**只留 MeshRenderer = 2.795%**（见第四节 4.3）。

⇒ 不需要再去比 `boneWeights`。原猜测（"顶点权重集中 + bindpose 近单位所以退化后仍可见"）**不成立**。
⇒ 结论反而更强：**「非 null 骨骼 = 0 ⇒ 不画」没有反例**，可以当硬判据用。
⇒ 修复方案 A（换 `MeshFilter` + `MeshRenderer`）对 `MoGu` 也适用，而且**它已经有一半是这么干的**。

---

## 六、本轮探针与产物

| 文件 | 回答什么 |
|---|---|
| `Tools/cs/chk_mat_state.cs` | 材质槽 null 数（材质到底丢没丢） |
| `Tools/cs/dg_enemy_mesh.cs` | 网格完整性：`subMeshCount` / `GetIndexCount` / 大余量取景 |
| `Tools/cs/dg_enemy_matattr.cs` | 材质属性全量 dump + **交叉互换**实验 |
| `Tools/cs/dg_smr_diff.cs` | SMR 序列化字段 diff（★ 用了 `Next(false)`，有盲区） |
| `Tools/cs/dg_meshbounds.cs` | `renderer.bounds` vs `mesh.bounds` 取景对照 + 超大视野兜底 |
| `Tools/cs/dg_bones_probe.cs` | **bones 的 null 元素数** + shaderKeywords + MeshRenderer 直画实验 |
| `Tools/cs/dg_bindpose.cs` | bindposes 统计 + 单位矩阵化实验 |
| `Tools/cs/dg_smr_rebuild.cs` | **T1/T2/T3 三档对照（决定性）** |
| `Tools/cs/dg_smr_deep.cs` | 完整展开 dump + diff + blendShape 清空实验 |
| `Tools/cs/dg_ab_shot.cs` | **骨骼普查 + 同取景 A/B 对照图** |
| `Tools/make_compare_ab.py` | 把 A/B 拼成可读证据图 |
| `Tools/cs/bf_tree.cs` | **★ 只读 dump prefab 完整层级 + 源 FBX，逐 SMR 报 bones/rootBone/mesh/材质** |
| `Tools/cs/bf_fix.cs` | **★ 修复脚本：按源 FBX 顺序把骨骼接回（预演 + APPLY 两档）** |
| `Tools/cs/chk_enemy_vis.cs` | **★ 收口判据（三条并列，已加「逐元素数 bones 非 null」）** |
| `Tools/cs/q_items.cs` | 列演示场 `_items`（查 prefab 名笔误 / 条目数） |
| `Tools/cs/q_comp.cs` | 查 `isPlaying` / `isCompiling`（改完脚本后确认编译真的开始了） |

**结论图**：
- 修复前：`Tools/screenshots/enemies/_对比_同网格_SMR空白_vs_MeshRenderer画出.png`、
  `Tools/screenshots/scene/_修复前_四对象_开vs关.png`
- 修复后：`Tools/screenshots/scene/_修复后_四对象_开vs关.png`、
  `Tools/screenshots/scene/_验收_演示场面板逐条目召唤_六个敌人.png`
