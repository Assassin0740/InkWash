# InkWash 数值参考（长期记忆的细则部分）

> 由 `.workbuddy/memory/MEMORY.md` 拆出：这里放**稳定的现状数值与硬规矩细则**，随开发推进才更新；
> 易变的「当前进度 / 下一步 / 未决」留在 `MEMORY.md`。坑与成因案例见 `Docs/工程坑表.md`。

## A. 动画 / 龙 / 战斗

- **换模型 = 换骨架**：换 `avatar`；容器 `localScale` = 目标身高 ÷ 原生身高、`localPosition.y` 重定；`stateOffsets` 整表重测｜`FootIK` 竖直偏移只能走**容器 `localPosition.y`**｜**骨数必须等于 `mesh.bindposes` 数**｜重建/回退 prefab 用 `InstantiatePrefab`+`ApplyPrefabInstance`（**不要 `LoadPrefabContents`**）；拓扑 `Idle/Walk/Run/Dash` + `Atk1→Rec→Atk2→Rec→Atk3`，转移**按列表顺序求值、先满足者胜**（带 Trigger 的必须排在移动阈值之前），过渡期只认 `GetNextAnimatorStateInfo`
- **`PlayerController` 注入输入是「相机相对」的**：喂世界方向会歪 ~55° ⇒ 剑砍空气。正解 `Vector2(Dot(dir,right), Dot(dir,fwd))`
- **墨龙**：`TickChase` 覆写成盘旋（`TickCircling`）；攻击 `Tell(0.35)→Dive(0.45)→Strike(0.22)→Recover(0.58)`；高度**只由 `_currentLift` 一处真相**写进容器 `localPosition.y`
- **龙真值**：2 SMR（`Object_281` 28082v + `Object_282` 804v）、`bones=273`、`rootBone=_rootJoint`、`M_Ink_Boss_Dragon`、bounds `5.871×2.588×6.044`、**`_spine` 24 节 `drgon_03`→…→`drgon_00`（i 小 = 尾/根侧）**、`_modelRoot=Visual`、`_baseRel.Count=24`、`_limbRoots.Count=19`（脊柱外的肢体分支骨）、像素 9.43/9.14%；`Muzzle` ↔ 尾尖 **7.765 m**，头在 **+Z**，摆位只摆 XZ
- **四把尺**：握拳度（2.2 摊开／1.2 握拳）｜剑尖总行程｜脚位波动｜跨立（横 0.52 / 纵 0.29 m）
- **★ `_limbRoots` / `_limbDriven` 真身（09-18 实测，19 条里只有 4 条是腿）**：
  - **腿 ×4**：`drgon_0234` / `drgon_0253`（父 = `脊柱[0]`，后腿）、`drgon_0154` / `drgon_0175`（父 = `脊柱[13]`，前腿）；
    伸展 0.82~1.08 m，`|首子方向 · 容器前向|` = **0.10~0.49**
  - **尾巴 ×1**：`drgon_0206`（父 = `脊柱[0]`）—— 子树 **28 骨**、伸展 **3.24 m**、`|dot|` = **0.99**（沿身体轴）
  - **头饰簇 ×14**：`drgon_054` / `061` / `064` / `093` / `0100` / `0103` / `0106` / `0109` / `0114` / `0120` / `0123` / `0126` / `0129` / `0138`
    （父 = `脊柱[22]`）—— 3~17 骨、伸展 0.24~0.70 m，首子方向朝各方向（鬃 / 须 / 角 / 颌）
  - 筛选口径：`limbHeadExcludeLinks = 2`（父节 ≥ `Count-2` ⇒ 头饰簇）+ `limbTailAxisDot = 0.80`（`|dot|` ≥ 阈值 ⇒ 沿身体轴 ⇒ 尾巴）
  - ★ 尾巴与后腿**共用父节点** `脊柱[0]` ⇒ 只靠父节序号判不开，**必须另看方向**
- **★ 头/尾/四肢「姿态校正」基线（第十八轮 09-18 实测，`reports/drg_fix.txt`）**：
  - **基准姿态**（`swimWaveAmp = 0` + `limbSwingDeg = 0`）与**驱动姿态逐位相同** ⇒ 下列全是模型原生姿态：
    尾尖绝对抬升 **+0.265 m**（中段累计 **+0.799 m**）、尾逐节 pitch **−6° → −33°**、
    四条腿首段 `dot(容器 fwd)` = **0.490 / 0.283 / 0.099 / −0.351**（前三条往前伸）
  - **头簇长轴（PCA）**：`_spine[Count-2]` 之下、非 `_limbRoots` 的 **50 枚**骨；
    长轴（容器系）`(−0.584,−0.503,−0.637)`、**俯仰 30.21°**（轴朝下）；
    展布 **0.864 m** / 垂直半径 **0.359 m**（扁平度 **2.41** ⇒ 长轴可信）⇒ 抬头 **30°** 归正
  - **尾巴主轴链**：`_tailChain` **13 节 / 3.24 m**（每层取"与上一段夹角最小"的子节点 ——
    取"离根最远"会拐上横向短鳍）
  - 校正后判据：尾逐节 max\|pitch\| **0.00°**｜尾尖 Δy **0.000 m**｜尾尖横向摆幅 **0.686 m**
    ｜腿 `dot(fwd)` **−0.79/−0.32/−0.80/−0.10**、首段方向变化 **38~78°**｜脊柱 ySpread **0.000 m**
  - **循环性**：周期 = `1/swimWaveFreq` = **2.222 s**（`swimWaveFreq 0.45`）；
    把 freq 临时设 1/3 Hz（周期 = 90 帧整数）测：整周期残差 **1e-5 m** vs 相邻帧 **2.7e-2 m** ⇒ 严格周期
  - 新增字段默认值：`headAlignPitchDeg 3`／`Yaw 0`／`Roll 0`／`headAlignLinks 2`／
    `tailLevel 1`／`tailWaveAmp 0.35`／`tailWaveSpan 0.5`／`tailPhaseLagDeg 0`／`limbSweepBackDeg 60`
    ★★ `headAlignPitchDeg` **现出货值 = `0`（第二十六轮改回）**。演进：
    `30`（第十八轮，坏尺子）→ `3`（第二十一轮"吻部水平"，**用户仍说抬头**）→ `−12`（第二十二轮人眼定档）
    → **`0`（第二十六轮：换基后归零）**。
    ★★ **`−12` 已作废**，理由是结构性的：那个 −12 是在**歪基准**上挑出来的 ——
    `_baseRel` 采在 `StraightenSpineBase()` **之后**，而拉直把**头载体** `_spine[Count-2]`
    也转了 **93.37°**（第二十六轮实测，见 `工程坑表.md` 48）。
    换基（`headLockUseRestPose = true`）后，`pitch = 0` 的头载体偏差**恰为 0.00°** ⇒ 真正回到 FBX 原生姿态。
    源码默认值与 prefab 覆盖值同步（`check_prefab_overrides` 同步为 0；现 120 字段级）。
    **注意「0」在这里是"零调整"而不是"零仰角"**：模型原生基准本身吻部低 ~3°（≠ 水平），
    换基后这一层零点偏置（见本文件末条与 `工程坑表.md` 36）**在"锁基准姿态"路线下不再需要人为补偿**。
- **★ 尾巴 = 身体 sin 波形的「延续」（第十九轮 09-18，`reports/drg_tail19.txt` + `drg_kinkbase.txt`）**：
  - 旧版两个缺陷（用户反馈「尾巴还是没有跟上 sin 的」）：① 尾巴铺的是**第二条独立的波**
    `amp·u·sin(2π·span·u+ph)`，其 u 是"尾根→尾尖"，波峰同样往 u=0 走 ⇒ 传播方向是**尾尖→尾根**，
    与身体的**头→尾**相反 ⇒ 两条波**对着撞**；② **接点切向不连续**：接点两侧一阶导的横向分量之比
    = `π·amp·gain : tailWaveAmp` = **3.61 : 0.35（差 10 倍）** ⇒ 身体尾端在摆、尾根几乎不动。
  - **修法**：把身体曲线 `SerpPoint` 的参数**往 u<0 外推**当尾链中心线（去掉 `Mathf.Clamp01`）
    ⇒ 波长 / 相位 / 传播方向 / 接点切向**按构造相等**。
    ★ 外推时 `sin(πu)` 在 u<0 取**负值**，这个负号正是接点能连续的原因（= 自动反相），
    **绝不能取绝对值**；★ 原点与轴向必须与脊柱**同源**（`_spine[0].position` + 同一个 `fwd`）。
  - 新字段：`tailFollowBodyWave true`（false = 回退旧独立行波，攻击相位 `ApplySpineOffsetsRaw` 走这条）、
    `tailFollowGain 0.9`。**折返上限**：`amp·gain·6.02/Σ骨长 < 1`。
  - 实测：接点折角 20.67° → **6.11°**；尾尖横摆 p2p **0.686 → 1.982 m**；身体中段 1.750 m（两模式一致 = 尺子自检）。
- **★★ 判「接点有没有断」必须对照「尺子本底」（第十九轮新规矩）**：链是**按弦驱动**的，弦方向相对真实
  切线偏 `≈ ½·lat''·Δu/T`，首阶只与**段长**有关 ⇒ 接点两侧的弦必然分居切线两边，夹角**不可能为 0**。
  同刻三组实测（`drg_kinkbase`）：身体相邻节夹角均值 **10.16°**（max 31.3）／尾巴相邻节 7.16°／
  接点 **6.11°**（follow）⇒ 接点比身体自己的内部节间角**还小** = 物理连续；
  而修前接点 20.67° = 身体内部的 **2.04 倍** = 统计离群 = 真断。**只看 6.11° 这个绝对值会误判成"没修好"。**
- **★★ 头部俯仰已「量得出来」了（第二十一轮 09-18，`reports/drg_head21.txt` + `drg_headfinal21.txt`）**：
  - 前两把尺子都作废：第十八轮 `PCA 长轴 30.2°`、第十九轮 `头簇质心仰角`（0° 时报 −36.4°）
    —— 二者量的都是**质量分布方向**（颈+颅、或头簇质心），**不是吻部朝向**。**30 这个值本身就是病根。**
  - 现在这把尺子：`SkinnedMeshRenderer.BakeMesh()` 取**当前姿势顶点**（`mesh.vertices` 因 FBX
    关 Read/Write 恒空），在**正中矢状面**（`|lat| <= latMax·0.22`）里取**最前 2%** 质心当吻端、
    **最后 12%** 质心当颈根后，两者之差 = 吻部轴。
  - ★★ **必须锁索引**：头区/正中面/吻端/颈根**只选一次**（以后每帧只用这些索引）。
    不锁的话 `pivot` 随脖颈偏航平移 ⇒ 切平面在头上来回扫 ⇒ 集合每帧在变。
    实测同一批姿态：**不锁 → −33.85/+15.87/−33.29°（极差 49.7°，假的）**；
    **锁 → 6 相位 −3.03/−3.03/−3.03/−3.04/−3.04/−3.03°（极差 0.01°，真的）**。
  - **定档换算**：`吻部仰角 = 0.9637 × headAlignPitchDeg − 3.09°`（6 档线性拟合，残差 < 0.1°）。
    `0` = 模型原生基准姿态（吻部低 ~3°，**≠ 水平**）；**`3.2` = 吻部水平**；
    `30` = 吻部上抬 **25.8°**、颅顶 **54°**。出货值 **3** ⇒ 6 相位仰角 0.19/0.22/0.16/0.26/0.01/−0.13°（极差 0.39°）。
  - **★ 头不会随行波俯仰**：`SerpPoint` 的 `fwd`/`right` 都过 `Flat()` ⇒ 中心线整条在水平面内
    ⇒ `FromToRotation` 只能绕竖轴 ⇒ 链上任何一节都不产生俯仰。实测与之一致。
    巡游中头只做**偏航 ±30°**（相位 0.00→0.833 的吻部偏航：13.3 / −10.3 / −16.6 / −0.2 / 24.4 / 29.3°）。
  - 尺子精度：**重复性 0.01°、绝对精度 ±0.5°**（锁帧不同 ⇒ 索引集略异，同一姿态两次跑差 0.4°）。
    ⚠ **但精度 ≠ 正确性**：见下一段"零点偏置"。
  - 交付图：`Tools/screenshots/enemies/_墨龙_头部_改前改后_v1.png`（30 vs 3，侧视 + 俯视3/4 + 前侧3/4）、
    `_墨龙_头部档位_绝对值_v2.png`（0/3/5 与 −5/−15/−23/−30 的档位图鉴）。
- **★★★★ 第四把尺子：零点有约 15° 的「朝上偏置」（第二十二轮 09-18，`reports/drg_headscan22.txt`）**：
  - 用户第二次反馈 **「改后还是抬头的，还是得改」** —— 而那时尺子报的是**吻部仰角 +0.3°（水平）**。
  - 真因：这条「吻部轴」是 **颈根后 0.15 m → 吻端** 的**弦**，而龙头的**颅骨/角/鬃整块长在弦的上方**
    ⇒ **弦水平时，眼睛看到的仍然是一颗"架在脖子上仰着"的头**。
  - ⇒ **斜率 0.9637 可信（相对档位比较可用）；零点不可信**。
    **绝对档位一律由「十档扫描出图 + 真实世界水平线 + 人眼」裁决。**
  - 十档实测（相位 0.50，`drg_headscan22`）：
    `+3 → −0.31°`｜`0 → −3.20°`｜`−3 → −6.08°`｜`−6 → −8.98°`｜`−9 → −11.87°`｜
    **`−12 → −14.77°`（定档）**｜`−15 → −17.63°`｜`−18 → −20.58°`｜`−21 → −23.48°`｜`−24 → −26.40°`。
    对应颅顶仰角：`+3 → 29.3°` … `−12 → 15.1°` … `−24 → 3.8°`（斜率 ≈ 0.937）。
  - ★ **`pitch = −12` 时仰角随相位有 1.63° 摆动**（−14.77 ~ −13.14），而 `pitch = 0` 时是 0.01°。
    原因：`q = headExtra * q` 是**左乘、容器系**，固定 pitch 与随行波变化的 yaw **不可交换**。
    量级无碍定档，但**别把"0 档刚性"外推成"任何档位都刚性"**（坑表 38）。
  - 实测定档值（−12）下的巡游：吻部偏航 **−17.2 ~ +30.4°**（行波在跑）、仰角极差 1.63°。
  - 流水线（工具已固化）：`Tools/cs/drg_headscan22.cs`（十档出图）→
    `Tools/make_head_scan22.py`（叠真实水平线 + **裁到头部放大 1.7×** ⇒ `_墨龙_头部_定档扫描_v4.png`）→
    `Tools/make_head_gen3.py`（三代同框 ⇒ `_墨龙_头部_三代对照_v5.png`）。
    **放大是必需步骤**：900x600 全景里龙头只占 ~180 px，缩略图上分不出 15°。
  - 交付图：`_墨龙_头部_定档扫描_v4.png`（十档挑选图）· `_墨龙_头部_三代对照_v5.png`（30 / 3 / −12）·
    `_墨龙_头部_放大对照_v3.png`（六档裁头放大）。
- **★ 龙身尺度（第十九轮实测）**：`Σ_segLen` = **8.266 m**（24 节；首段 0.237 / 平均 0.359）；
  尾链 13 节 **3.24 m**（首段 0.246 / 平均 0.297）；`_spine[Count-2]` 头簇子树 **124 骨**、包围球半径 **1.06 m**
- **★★★★ 头部换基：锁「拉直之前」的 FBX 原生姿态（第二十六轮 09-18，`reports/drg_headfix.txt`）**：
  - 用户第三次否掉头部：「这三个都是歪的，ABC，全是歪的」（A 不锁 / B 锁 `pitch=0` / C 锁 `pitch=−12`）
    ⇒ **三档共有的那一项才是真因**。真因：`_baseRel` 采在 `StraightenSpineBase()` **之后**，
    而拉直把**头载体** `_spine[22] = drgon_025` 也转了 **93.37°**（段方向偏 **66.56°**，
    `reports/drg_headref.txt`）。头簇（39 叶 + 鬃/须/角/颌）刚性且从不被驱动
    ⇒ **头的世界朝向 ≡ 该骨世界旋转 × 常量** ⇒ 三档全在同一个歪基准上。
  - 修法：新增 `_restRel` + `CaptureRestRel()`（**必须排在 `StraightenSpineBase()` 之前**）与
    `headLockUseRestPose`（默认 **true**）。两处锁定写点 `ApplySpineOffsetsRaw` / `ApplySerpentineSpine`
    必须同源。蛇形驱动仍用拉直后的 `_baseRel`/`_baseSegLocal`（那才是"它是条蛇"的来源）。
  - **实测（同一物件就地摆回 FBX 姿态，同机位/材质/光照，唯一变量=姿态）**：
    头载体容器相对旋转偏差 ｜ `rest(FBX) 0.00°`（俯仰 −46.9 / 偏航 +86.6 / 滚转 +0.0）
    ｜ `old = 138.97°`（−36.6 / +84.2 / **−175.6**）｜ **`new = 0.00°`（−46.9 / +86.6 / +0.0）**。
    新档**三相位（0.00/0.30/0.60）完全相同**，且与基准帧**逐分量相同**。
  - 自检：头簇 8 枚叶子骨局部旋转在四档之间恒 **0.0000°**（头是刚性的）；
    头簇相对挂点 `Δdir ≤ 0.03°`、`Δrot 0.000°`（**锁定没有把头的形状拧变形**）。
  - **闭合**：`headAlignPitchDeg = 0` 时偏差**恰为 0.00°** ⇒ 第二十二轮那个 `−12` 是在歪基准上挑的，作废。
  - ★ 跨实例标尺（`reports/drg_scale26.txt`）：`Z_Enemy_MoLong.prefab` 根 `localScale` **1.0000**
    （骨骼 `lossyScale` 0.0024）vs `Z_Dragon.prefab` 根 **0.7546**（骨骼 0.0018）
    ⇒ `1/0.7546 = 1.3252` = 报表里恒定的「半径比」；未归一化的位置差恒 **0.1935 m** 即由此而来。
- **★ 风暴特效（第二十三轮 09-18，`reports/drg_storm23.txt`）**：
  - 条目映射：盘旋 idx=23 / 俯冲撕咬 24 / 扫尾 25 / **吐息 26**。
  - **吐息：口部喷 592 次 ｜ 沿链喷 0 次**（用户定案「只有口部向前喷」⇒ 沿链必须 = 0）**✓**
  - 盘旋：活跃电弧条数恒 **3**（min=max=3，90 帧）；弧带可见像素 侧视 **810** / 前3/4 **1203**（≥300 ⇒ 没被龙皮挡掉）。
  - 撕咬：电弧条数均值 5.24（max 10，156 帧）；相位分组 Tell 4.00 → Dive 6.31 → **Strike 9.87**（峰值在打击相）；
    ★★ **烟里有没有电弧（判据 4 语义窗口）= 113/125 = 90.4%**（要 ≥80%）；链节覆盖 **24/24**（俯冲段应 = 24）。
  - 身长（判据 6）**7.409 ~ 8.266 m**（均值 7.44~7.67，要 7.3~8.1）⇒ 特效没把姿态带歪。
  - 帧时间：特效开 5.80~7.70 ms，>16.7 ms **0~1 / 90** ⇒ 达标。
  - ⚠ **判据 7「0 B/帧」未达标：三段恒定 ~20.5 KB/帧 —— 但「特效关」也 20527 B/帧，两态几乎逐字节相同
    ⇒ 归因不在风暴特效**（在别处）⇒ **待查**（不要记成风暴的锅）。
  - ⚠ 吐息条目「烟浓度(0~1) 均值 0.000」但「活烟团数 24.0」自相矛盾 ⇒ 浓度那把尺子在吐息档不敏感，**待查**。
  - 历史探针 `reports/drg_storm_ab.txt`（弧带看不见的归因）：**已作废** —— 它跑在修复前，
    五档（bias 0/0.8/2/3 × ZTest 4/8）「走廊内青」**全 0** ⇒ 掩码本身盲了（材质改饱和青后阈值没跟）。
    弧带可见性一律以 `drg_storm23` 的「弧带可见像素」为准。
  - 录像 `Tools/screenshots/enemies/S23/_墨龙风暴_v1.mp4`（1.54 MB，150 帧 @30 fps）。

## B. 墨色系统（`InkCharacter.shader` / `InkStylePanel`）

- **墨彩回填** `color *= 1 + (albedoHue − luma)·_ChromaKeep·2.2`（只改色相不改明暗）；**`2.2` 为主角近黑贴图标定 ⇒ 套到高饱和贴图过曝成彩虹**（龙 0.45→**0.10**）
- **`_BandBias` 负太多 ⇒ 暗部塌进焦墨、连颜色一起失掉**；抬过头 ⇒ 发白。**浓淡轴不能单独承担层级区分**，优先用**冷暖**。现状：主角 **+0.15** 淡墨／六敌人 **0.00**／墨龙 **−0.34** 浓墨
- **线粗细是「屏幕」属性** ⇒ `px ≈ 300×宽度 + 1.6`：主角 **0.012（5 px）**／剑 0.009／敌人 0.018／龙 0.018／高台 0.026｜**敌人描边 = 朱砂红线性 `(0.48,0.035,0.022)`**；`Material.SetColor` 在 Linear 空间**不做 sRGB→Linear 转换** ⇒ 必须给**线性值**
- **`_HueKeep`**：明度守恒色相注入 `color = luma(墨阶色) × albedo/luma(albedo)`。**默认 0 = 旧行为逐像素一致** —— shader **全场共用**（主角/龙/剑/场景）⇒ 新增参数必须默认零影响。现状：六敌人 **1.0**／主角·龙·剑 **0.00**
- 大平面**不许**走墨阶量化（`steps<0.5` 必须 `saturate(x)`）；墙/地/台/檐/道具 `_Bands=1`｜**龙**：`_BaseMap` 清空、`_ChromaKeep 0.10`、`_InkDensity 0.25`、`_Bands 4`、赭墨暖褐

## C. 资产 / 素材（**不入库**，靠 install 脚本 / 介质复原）

- **主角** Char_Feng「锋」→ `Tools/install_feng.py`｜**武器** W_Sword ≈1.198 m → `install_w_sword.py`（`20003228`）｜**敌人** Sketchfab 中文志怪 ×5 → `gltf2fbx_scaled.py`（上游 FBX 在 `ThirdParty/Ziyuan/_fbx/`）｜**动画库** Quaternius UAL1/UAL2｜QFramework + PrimeTween 1.4.11（**不用 DOTween**）
- **KayKit** `Skeleton_Golem/Minion/Mage.fbx` = `Enemy_MoYan/MoTu/MoOu` 的 Visual｜AudioKit 在 `BeforeSceneLoad` 换 ResKit、`AfterSceneLoad` 换回；**纯 2D**｜**跨机闭包 = 106 文件 / 170.9 MB**（ThirdParty 全量 640 MB），`transfer_closure.py`/`scene_deps.py`/`verify_transfer_closure.py`，**必须连目录自身 `.meta`**
- **`Main.unity` 外部依赖 58 条**：20 个自然道具 = Quaternius / StylizedNatureMegaKit 的 13 个 FBX ＋ `Player.prefab`；**这就是那边 Build 报错的真因**｜**项目原本无任何粒子资源**（特效全程序化，仿 `InkHitVfx.cs`：场景单例 + 静态入口 + 惰性池）—— 第十五轮起决定引入外部粒子包

## D. Ziyuan 素材与可见性度量

- **单位极乱**（×124.69 ~ ×0.0024）⇒ Blender 侧归一 + Unity 侧按**顶点真值**微调，别跨工具对账。`Z_Enemy_MoLong` Generic 9.000 m／50 552 面｜`Z_Orge` 2.600／17 309｜`Z_Orc` 2.050／21 590｜`Z_Undead` 1.949／16 743｜`Z_Dragon_LP` 6.999／6 157｜**`mixamorig:`/`Bip001` 可自动 Humanoid**；**蛇形龙不能 Humanoid**（报 `'Head' not found`）⇒ Generic + 程序驱动｜**龙无 body albedo，颜色画在 glTF `emissiveTexture`**；骨名 `drgon_0xx`、2 Mesh `Object_281/282`、`bindposes=273`
- **可见性度量的正确口径**：**`Showcase.unity` + 演示场相机** + **同 tick 背靠背差分帧**（渲染器开/关逐像素差，两帧都关投影），**不用哨兵底色**。两条保险：**在视锥内**（`TestPlanesAABB` + 报相机坐标）、**同批放阳性对照**。工具 `dg_sc_verify.cs`★／`ev_verify.cs`／`ev_scan.cs`／`ev_tune2.cs`。**⛔ 绝不用洋红**
- **定标脚本的相机必须每帧重设**（演示场脚本会在 `Update` 里拉回去）｜**`mesh.vertices` 在本工程返回空数组** ⇒ 读顶点真值不可用；`renderer.bounds = localBounds × lossyScale` 是推的

## E. 验收基线 / 里程碑 / 性能 / 遗留

- 场地：`StageAnchor` = **(0, 0.05, −8)**｜院墙 ±22／石门 ±10／`Arena` 高台 ±5（仅 0.30 m）｜**`WaveSpawner` 生成点必须与玩家同层**｜预览图用**独立 Layer 31 + `cullingMask`**
- 规模：F1 自动化 **208 项**；S1 31 · S2 54＋姿态 4＋音频 6 · S3 22 · S4 32 · S5 59｜**M6 ≥0.30**／**M7 轮廓 3.5~6 px**／**M9 彩度净贡献 ≥0.03**｜演示场 `ActionShowcase`：`PlayForCapture(index)`／`ItemCount`（现 **29**）／`ItemLabel`／`StopAutoClose()`
- **里程碑**：M0~M4 ✅｜**M5 ⬜ S5 待重跑**｜M6 进行中（F1 208 项全绿、F2 完成、**F3 对象池无收益不做**）｜M7 推进中
- **性能（09-17 编辑器内 Play，1080p）**：静置 5.73 ms（174 fps）｜战斗 6.16 ms（162 fps）、0/1603 帧 >16.7 ms｜**`GC.Alloc` 跨会话不可比** ⇒ 只做同会话相对比较
- **遗留**：主角受击动作（`PlayerHealth.Damaged` 零订阅者）｜第 4 段攻击/拉拽/格挡/主动技能（现 10 个 `SkillData` 全被动）｜命中音效未接线｜跑步 5 cm 浮空 · 步幅 0.25（真人 ≈0.43）· 骨盆偏航 50.5° · 主角 2.17 m 偏高｜`_SpecStrength 0.5` 亮边抢边缘｜主角贴图"数码迷彩"斑块
